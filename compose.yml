services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: telegram-news-classifier
    env_file: .env
    volumes:
      - ./config:/app/config
      - ./state:/state
    stdin_open: true
    tty: true
    restart: unless-stopped
